{% extends 'base.html.twig' %}

{% block title %}Cargar una nueva venta{% endblock %}

{# {% form_theme form _self %} #}

{# {% block _post_type_venta_nombre_widget %}
    <div class="integer_widget">
        {% set value = value|default('number') %} 
        {{ block('form_widget_simple') }}
    </div>
{% endblock %} #}

{# {% block _post_type_venta_fecha_widget %}
    <div class="integer_widget">
        {% set value = value|default('01-01-2020') %} 
        {{ block('form_widget_simple') }}
    </div>
{% endblock %} #}

{% block body %}

<div class="alert alert-info" role="alert">
    <h5><i class="fas fa-plus"></i> &nbsp;  Crear un nuevo producto</h5>
</div>

<div class="col-md-8 offset-md-3 ">
 

 {{ form_start(form_producto_venta, { attr: { class: ' form-inline' }, 'action': path('crear_producto_venta') }) }}
                        
                        
                        {{ form_row(form_producto_venta.precio_costo) }}
                        {{ form_row(form_producto_venta.producto_id) }}
                        {{ form_row(form_producto_venta.venta_id) }}
                        
                         <div class="input-group mb-2 mr-sm-2">
                            <div class="input-group-prepend">
                            <div class="input-group-text"><i class="fab fa-product-hunt"></i></div>
                            </div>
                            <input type="text" class="form-control" id="producto-elegido" placeholder="Producto" readonly="readonly">
                        </div>
                        <div class="input-group mb-2 mr-sm-2">
                            <div class="input-group-prepend">
                            <div class="input-group-text">#</div>
                            </div>
                            {# <input type="text" class="form-control" id="inlineFormInputGroupUsername2" placeholder="Username"> #}
                            {{ form_row(form_producto_venta.cantidad) }}
                        </div>
                        {{ form_row(form_producto_venta.submit) }} 
                        {# {{ form_widget(form_producto_venta) }} #}
                            <a href="#" id="cerrar-producto-elegido" class"btn btn-info">
                                <i class="far fa-2x fa-times-circle"></i>
                            </a>
                        {{ form_end(form_producto_venta) }}


<div>

{% endblock %}
